### Stored for documentation and future model benchmarking purposes.

There's an MMORPG that i'm working on. It's very old (late 90s and early 2000s). It doesn't have a traditional database like MySQL and the game data (objects, maps, NPCs, spells, characters) is actually stored in text files with .DAT and .CHR extensions. For now we'll just focus on parsing the .CHR (Charfile) data, but keep in mind I'll also ask you to parse the .DAT files in the future. Each character in the game has its own charfile, there's a .chr.bk file as well, which is a temporary copy of its .CHR equivalent... you must ignore ALL the .chr.bk files.
There could be tens of thousands of .CHR files and this script will run on the same VM as the game server, therefore performance is a top priority. Write idiomatic Rust code following best practices and apply defensive programming to handle edge cases gracefully. Consolelog the amount of seconds that it took to parse the .CHR files, how much time it took to populate the DB, and any other step you think I'm missing. This project will be used by spanish speaking Argentinian people, so consolelog it in spanish. You should still use english for variable names/functions and everything else. The PostgreSQL DB will be hosted on a different VM.

I'd like to have a Rust script that parses the .CHR files from the Charfile folder and then inserts them into a PostgreSQL database. Provide 10 possible names for the database where we'll store all the relevant game data (.DAT, .CHR, etc). This MMORPG has two main versions: the original -called Alkon- was written in Visual Basic 6, and the other one -called Dakara- was written with C++. This is relevant because the .CHR files have different fields depending on which version you're using. You must support both. You should also store an additional field called "NAME", you can pull this value from the name of the file itself - keep in mind the name can have spaces, like for example EL TRABAJADOR.CHR - There are some sensitive fields I want you to skip in the parsing process, they are: "EMAIL", "PASSWORD", "PASSWORDHASH", "PASSWORDSALT", "LASTIP1" (can go up to LASTIP5).

You have access to the context/ALKON_13.3_VB6 and context/DAKARA_13.3_CPP folders with a couple of .CHR and .DAT files so you can read, understand, and compare their fields. That's just for informational purposes, the files you must parse are at the ao-data-to-sql/Charfile directory. Reading all the files in the context folder will help you to properly set up a maintainable codebase architecture. I want to avoid the situation in which once I ask you to start parsing some .DAT files you end up having to refactor the project's architecture. Modularity and reusability is very important.

As a sidenote, in the future I'll also need a different Rust script that takes care of running certain SQL queries and then storing the outputs in .JSON files. I thought about uploading this script on a different repo, but I know for a fact that we'll reuse a lot of external crates accross these 2 scripts/crates, and probably share some functions. Is this relevant? Would you have separate repos? What's your recommendation? I'm from the TypeScript ecosystem so words like "monorepo" and "pnpm" come to mind, but I'm not an expert on the Rust ecosystem.

A key issue: this MMORPG is an open-source project with hundreds of forks throughout history, so not all .CHR or .DAT files will look the same. Some have more fields, some changed the original names of the fields, etc. And this script should work out of the box for those forks too, basically I don't want to force server admins using this script to modify long type/interfaces in the source code to adapt the script to their own fork. Non-technical server admins should be able to use this script with little to no effort. Tell me how would you handle this problem, with 3 alternatives.

You must also take care of dockerization. Just FYI, the VB6 version of the game server is hosted on Windows Server 2012+ VMs, and the C++ version is hosted on Ubuntu VMs. Don't use "latest" or a keyword like that in the dockerfile, specify an exact version. In the "context" folder you have the Cmake data so you can pull the exact version of the libraries i'm using for the game server. Again: the PostgreSQL DB will be hosted on a different VM. In the future I'll ask you to create an E2E test suite with JSON fixtures and a temporary PostgreSQL DB that will be deleted once the test suite finishes, that way the tests don't tamper with my local DB. Don't implement this test suite now though.

Provide a thorough implementation plan that includes code snippets.